import { TestHostDirective } from './test-host.directive';

describe('TestHostDirective', () => {
  it('should create an instance', () => {
    const directive = new TestHostDirective();
    expect(directive).toBeTruthy();
  });
});
